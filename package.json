{
  "name": "safe-route-ai-root",
  "version": "0.1.0",
  "private": true,
  "description": "SafeRoute AI - Intelligent Urban Safety Navigation Engine for Pune Metropole",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}
